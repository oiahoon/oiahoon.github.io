# Local photography fallback

Public photography is normally uploaded to Unsplash and imported with:

```bash
npm run photos:unsplash:sync
```

Only place files here when a photograph should remain outside Unsplash, then run:

```bash
npm run photos:sync
```
