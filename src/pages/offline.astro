---
import BlogLayout from '../layouts/BlogLayout.astro';
---

<BlogLayout 
  title="离线模式 | Joey's Notes"
  description="您当前处于离线状态"
>
  <div class="max-w-2xl mx-auto text-center py-16">
    <div class="mb-8">
      <svg class="w-24 h-24 mx-auto text-gray-400 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M18.364 5.636l-3.536 3.536m0 5.656l3.536 3.536M9.172 9.172L5.636 5.636m3.536 9.192L5.636 18.364M12 2.25a9.75 9.75 0 109.75 9.75A9.75 9.75 0 0012 2.25z" />
      </svg>
      <h1 class="text-4xl font-bold text-gray-900 mb-4">您当前处于离线状态</h1>
      <p class="text-xl text-gray-600 mb-8">
        无法连接到网络，但您仍可以浏览已缓存的内容
      </p>
    </div>

    <div class="bg-blue-50 border border-blue-200 rounded-lg p-6 mb-8">
      <h2 class="text-lg font-semibold text-blue-900 mb-2">离线功能</h2>
      <ul class="text-blue-800 text-left space-y-2">
        <li>• 浏览已访问过的文章</li>
        <li>• 查看缓存的页面内容</li>
        <li>• 使用基本导航功能</li>
      </ul>
    </div>

    <div class="space-y-4">
      <button 
        onclick="window.location.reload()" 
        class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors"
      >
        重新连接
      </button>
      
      <div>
        <a 
          href="/" 
          class="text-blue-600 hover:text-blue-800 transition-colors"
        >
          返回首页
        </a>
      </div>
    </div>
  </div>
</BlogLayout>

<script>
  // Check online status
  function updateOnlineStatus() {
    if (navigator.onLine) {
      window.location.reload();
    }
  }

  window.addEventListener('online', updateOnlineStatus);
  
  // Auto-retry connection every 30 seconds
  setInterval(() => {
    if (navigator.onLine) {
      updateOnlineStatus();
    }
  }, 30000);
</script>
