# Agentic Coding Context

This file is kept for backward compatibility.

The Agentic coding context is now layered and routed through:

```text
docs/agent/index.md
```

Read `docs/agent/index.md` first. Do not load every file in `docs/agent/` by default. The index tells Codex, Claude Code, Cursor, Copilot, and other agents which smaller context file to read for each task type.
